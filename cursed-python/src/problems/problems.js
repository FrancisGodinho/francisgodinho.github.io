export default [
    {
        title: "Funky Functions",
        code: `
        funcs = [lambda x: i * x for i in range(4)]
        for f in funcs:
	        print(f(2)) 
        `
    },
    {
        title: "Funky Functions",
        code: `
        funcs = [lambda x: i * x for i in range(4)]
        for f in funcs:
	        print(f(2)) 
        `
    },
    {
        title: "Funky Functions",
        code: `
        funcs = [lambda x: i * x for i in range(4)]
        for f in funcs:
	        print(f(2)) 
        `
    },
    {
        title: "Funky Functions",
        code: `
        funcs = [lambda x: i * x for i in range(4)]
        for f in funcs:
	        print(f(2)) 
        `
    },
    {
        title: "Funky Functions",
        code: `
        funcs = [lambda x: i * x for i in range(4)]
        for f in funcs:
	        print(f(2)) 
        `
    },
    {
        title: "Funky Functions",
        code: `
        funcs = [lambda x: i * x for i in range(4)]
        for f in funcs:
	        print(f(2)) 
        `
    },
    {
        title: "Funky Functions",
        code: `
        funcs = [lambda x: i * x for i in range(4)]
        for f in funcs:
	        print(f(2)) 
        `
    },
]